<% if @feed_items.any? %>
<ol class="microposts">
	<% @tasks.each do | current_task| %>
	    <li id="<%= current_task.name %>">
	    <%= link_to "Go to task", task_path current_task, class: "btn btn-large btn-primary" %>
	<% end %>
</li>
</ol>
<%= will_paginate @feed_items %>
<% end %>
